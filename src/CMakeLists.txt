ADD_EXECUTABLE(lab main.cpp)

ADD_SUBDIRECTORY(Entities)
ADD_SUBDIRECTORY(Experiment)

TARGET_LINK_LIBRARIES(lab Experiment ROOT::Gui)

SET_TARGET_PROPERTIES(
        lab PROPERTIES
        RUNTIME_OUTPUT_DIRECTORY "${PROJECT_BINARY_DIR}"
)
